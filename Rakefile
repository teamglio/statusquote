require_relative 'lib/statusquote/random_quote_broadcast.rb'

desc "Broadcast a random quote to all users"
task :broadcast_random_quote do
	puts "Broadcasting..."
	RandomQuoteBroadcast.broadcast
	puts "Done!"
end
